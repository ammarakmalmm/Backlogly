import type { BacklogItem } from '../types';
const DB='backlogly-db', STORE='items';
const open=()=>new Promise<IDBDatabase>((resolve,reject)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE,{keyPath:'id'});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});
export const repository={async all(){const d=await open();return new Promise<BacklogItem[]>((res,rej)=>{const r=d.transaction(STORE).objectStore(STORE).getAll();r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})},async put(item:BacklogItem){const d=await open();return new Promise<void>((res,rej)=>{const r=d.transaction(STORE,'readwrite').objectStore(STORE).put(item);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})},async remove(id:string){const d=await open();return new Promise<void>((res,rej)=>{const r=d.transaction(STORE,'readwrite').objectStore(STORE).delete(id);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})},async replace(items:BacklogItem[]){const d=await open();const tx=d.transaction(STORE,'readwrite');tx.objectStore(STORE).clear();items.forEach(x=>tx.objectStore(STORE).put(x));return new Promise<void>((res,rej)=>{tx.oncomplete=()=>res();tx.onerror=()=>rej(tx.error)})}};
